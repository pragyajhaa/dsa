#include <iostream>  // Standard C++ header for input/output
using namespace std; // Allows using standard library names (like cout) directly

int main() {
    cout << "Hello, World!" << endl; // Print "Hello, World!" and a newline
    return 0; // Indicates successful execution
}